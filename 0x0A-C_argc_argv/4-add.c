#include <stdio.h>
#include "main.h"
#include <stdlib>
#include <ctype>
#include <string>

/**
 * check_num - check - sring there are digit
 * @str: array str
 *
 * Return: Always 0 (Success)
 */
int check_num(char *str)
{
	unsigned int count;
	count = 0;
	while
